<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="lzz.mybatis.demo.hello.dao.UserDao">

    <select id="findByUserNamelzz" parameterType="String" resultType="lzz.mybatis.demo.hello.model.User">
        SELECT * FROM user WHERE name = #{name}
    </select>
</mapper>